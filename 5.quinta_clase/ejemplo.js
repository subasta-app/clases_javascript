//complete la clase para que de esos resultados
let octData = [10,3,1,2,5,7,8,9,2]

const octAccounting = new Accounting( octData );
console.log('Promedio: ',octAccounting.average());
console.log('Total: ',octAccounting.total());
console.log('Impuestos: ',octAccounting.tax());
console.log('subTotal: ',octAccounting.subTotal());
